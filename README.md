# ErrorContext
Python stack traces tell you where things broke. ErrorContext tells you why, with no external dependencies. No separate logging setup. No configuration files. The exception carries its own context. Always.
